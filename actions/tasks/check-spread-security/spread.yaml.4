project: security-test

backends:
    test:
        type: adhoc
        allocate: |
            if [ ! -f already.ran ]; then
                touch already.ran    
                cp /home/ubuntu/.config/gcloud/application_default_credentials.json cred.json
                chown ubuntu:ubuntu cread.json
            fi
            ADDRESS localhost:22
        systems:
            - ubuntu-core-16-64:
                  username: external
                  password: ubuntu

path: /home/adhoc

restore:
    rm -rf /home/adhoc

suites:
    checks/: 
        summary: Verification tasks.

